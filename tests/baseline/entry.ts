// Widens the frozen baseline's surface: the pre-redesign envelope decoder for
// the differential's haystacks (the bundle inlines the frozen envelope).
export * from "../../src/index";
export { Envelope, envelopeFromBytes } from "@blockchaincommons/envelope";
