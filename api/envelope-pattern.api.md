## API Report File for "@blockchaincommons/envelope-pattern"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ArrayPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { BoolPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { ByteStringPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { Cbor } from '@blockchaincommons/dcbor';
import { CborDate } from '@blockchaincommons/dcbor';
import { CborInput } from '@blockchaincommons/dcbor';
import { DatePattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { Digest } from '@blockchaincommons/components';
import { Envelope } from '@blockchaincommons/envelope';
import { Interval } from '@blockchaincommons/dcbor-pattern';
import { KnownValue } from '@blockchaincommons/known-values';
import { KnownValuePattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { MapPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { NullPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { NumberPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { Pattern as Pattern_2 } from '@blockchaincommons/dcbor-pattern';
import { PatternRegex } from '@blockchaincommons/dcbor-pattern';
import { Quantifier } from '@blockchaincommons/dcbor-pattern';
import { RegexInput } from '@blockchaincommons/dcbor-pattern';
import { Reluctance } from '@blockchaincommons/dcbor-pattern';
import { Tag } from '@blockchaincommons/dcbor';
import { TaggedPattern } from '@blockchaincommons/dcbor-pattern/patterns';
import { TextPattern } from '@blockchaincommons/dcbor-pattern/patterns';

// @public
export const and: (...patterns: Pattern[]) => Pattern;

// @public
export const any: () => Pattern;

// @public
export const anyArray: () => Pattern;

// @public
export const anyAssertion: () => Pattern;

// @public
export const anyBool: () => Pattern;

// @public
export const anyByteString: () => Pattern;

// @public
export const anyCbor: () => Pattern;

// @public
export const anyDate: () => Pattern;

// @public
export const anyKnownValue: () => Pattern;

// @public
export const anyMap: () => Pattern;

// @public
export const anyNode: () => Pattern;

// @public
export const anyNumber: () => Pattern;

// @public
export const anyObject: () => Pattern;

// @public
export const anyPredicate: () => Pattern;

// @public
export const anySubject: () => Pattern;

// @public
export const anyTagged: () => Pattern;

// @public
export const anyText: () => Pattern;

// @public
export const arrayWithCount: (count: number) => Pattern;

// @public
export const arrayWithRange: (min: number, max?: number) => Pattern;

// @public
export type AssertionsPattern = {
    readonly variant: "Any";
} | {
    readonly variant: "WithPredicate";
    readonly pattern: Pattern;
} | {
    readonly variant: "WithObject";
    readonly pattern: Pattern;
};

// @public
export const assertionWithObject: (pattern: Pattern) => Pattern;

// @public
export const assertionWithPredicate: (pattern: Pattern) => Pattern;

// @public
export const boolean: (value: boolean) => Pattern;

// @public
export const byteString: (value: Uint8Array) => Pattern;

// @public
export const byteStringRegex: (regex: RegexInput) => Pattern;

// @public
export const capture: (name: string, pattern: Pattern) => Pattern;

// @public
export function cbor(valueOrPattern: CborInput | Pattern_2): Pattern;

// @public
export type CborPattern = {
    readonly variant: "Any";
} | {
    readonly variant: "Value";
    readonly cbor: Cbor;
} | {
    readonly variant: "Pattern";
    readonly pattern: Pattern_2;
};

// @public
export const compressed: () => Pattern;

// @public
export const date: (value: CborDate) => Pattern;

// @public
export const dateEarliest: (value: CborDate) => Pattern;

// @public
export const dateIso8601: (value: string) => Pattern;

// @public
export const dateLatest: (value: CborDate) => Pattern;

// @public
export const dateRange: (min: CborDate, max: CborDate) => Pattern;

// @public
export const dateRegex: (regex: RegexInput) => Pattern;

// @public
export type DcborResult<T, E> = {
    readonly ok: true;
    readonly value: T;
} | {
    readonly ok: false;
    readonly error: E;
};

// @public
export const digest: (value: Digest) => Pattern;

// @public
export const digestBinaryRegex: (regex: RegexInput) => Pattern;

// @public
export type DigestPattern = {
    readonly variant: "Digest";
    readonly digest: Digest;
} | {
    readonly variant: "Prefix";
    readonly prefix: Uint8Array;
} | {
    readonly variant: "BinaryRegex";
    readonly regex: PatternRegex;
};

// @public
export const digestPrefix: (prefix: Uint8Array) => Pattern;

// @public
export const display: (pattern: Pattern) => string;

// @public
export const elided: () => Pattern;

// @public
export const encrypted: () => Pattern;

// @public
export class EnvelopePatternError extends Error {
    readonly code: EnvelopePatternErrorCode;
    readonly details: EnvelopePatternErrorDetails;
    static emptyInput(): EnvelopePatternErrorTyped<"EmptyInput">;
    static expectedCloseBracket(range: Span): EnvelopePatternErrorTyped<"ExpectedCloseBracket">;
    static expectedCloseParen(range: Span): EnvelopePatternErrorTyped<"ExpectedCloseParen">;
    static expectedOpenBracket(range: Span): EnvelopePatternErrorTyped<"ExpectedOpenBracket">;
    static expectedOpenParen(range: Span): EnvelopePatternErrorTyped<"ExpectedOpenParen">;
    static expectedPattern(range: Span): EnvelopePatternErrorTyped<"ExpectedPattern">;
    static extraData(range: Span): EnvelopePatternErrorTyped<"ExtraData">;
    fullMessage(source: string): string;
    static invalidCaptureGroupName(name: string, range: Span): EnvelopePatternErrorTyped<"InvalidCaptureGroupName">;
    static invalidDateFormat(range: Span): EnvelopePatternErrorTyped<"InvalidDateFormat">;
    static invalidHexString(range: Span): EnvelopePatternErrorTyped<"InvalidHexString">;
    static invalidNumberFormat(range: Span): EnvelopePatternErrorTyped<"InvalidNumberFormat">;
    static invalidPattern(range: Span): EnvelopePatternErrorTyped<"InvalidPattern">;
    static invalidRange(range: Span): EnvelopePatternErrorTyped<"InvalidRange">;
    static invalidRegex(range: Span): EnvelopePatternErrorTyped<"InvalidRegex">;
    static invalidUr(cause: string, range: Span): EnvelopePatternErrorTyped<"InvalidUr">;
    is(code: EnvelopePatternErrorCode): boolean;
    static isEnvelopePatternError(value: unknown): value is EnvelopePatternErrorTyped;
    override readonly name = "EnvelopePatternError";
    static nestingTooDeep(maxDepth: number, range: Span): EnvelopePatternErrorTyped<"NestingTooDeep">;
    // @internal
    shifted(offset: number): EnvelopePatternError;
    get span(): Span | undefined;
    static unexpectedEndOfInput(): EnvelopePatternErrorTyped<"UnexpectedEndOfInput">;
    static unexpectedToken(kind: TokenKind, text: string, range: Span): EnvelopePatternErrorTyped<"UnexpectedToken">;
    static unmatchedBraces(range: Span): EnvelopePatternErrorTyped<"UnmatchedBraces">;
    static unmatchedParentheses(range: Span): EnvelopePatternErrorTyped<"UnmatchedParentheses">;
    static unrecognizedToken(range: Span): EnvelopePatternErrorTyped<"UnrecognizedToken">;
    static unterminatedRegex(range: Span): EnvelopePatternErrorTyped<"UnterminatedRegex">;
}

// @public
export const EnvelopePatternErrorCode: {
    readonly EmptyInput: "EmptyInput";
    readonly UnexpectedEndOfInput: "UnexpectedEndOfInput";
    readonly ExtraData: "ExtraData";
    readonly UnexpectedToken: "UnexpectedToken";
    readonly UnrecognizedToken: "UnrecognizedToken";
    readonly InvalidRegex: "InvalidRegex";
    readonly UnterminatedRegex: "UnterminatedRegex";
    readonly InvalidRange: "InvalidRange";
    readonly InvalidHexString: "InvalidHexString";
    readonly InvalidDateFormat: "InvalidDateFormat";
    readonly InvalidNumberFormat: "InvalidNumberFormat";
    readonly InvalidUr: "InvalidUr";
    readonly ExpectedOpenParen: "ExpectedOpenParen";
    readonly ExpectedCloseParen: "ExpectedCloseParen";
    readonly ExpectedOpenBracket: "ExpectedOpenBracket";
    readonly ExpectedCloseBracket: "ExpectedCloseBracket";
    readonly ExpectedPattern: "ExpectedPattern";
    readonly UnmatchedParentheses: "UnmatchedParentheses";
    readonly UnmatchedBraces: "UnmatchedBraces";
    readonly InvalidCaptureGroupName: "InvalidCaptureGroupName";
    readonly InvalidPattern: "InvalidPattern";
    readonly NestingTooDeep: "NestingTooDeep";
};

// @public
export type EnvelopePatternErrorCode = (typeof EnvelopePatternErrorCode)[keyof typeof EnvelopePatternErrorCode];

// @public
export type EnvelopePatternErrorDetails = {
    readonly code: "EmptyInput";
} | {
    readonly code: "UnexpectedEndOfInput";
} | {
    readonly code: "ExtraData";
    readonly span: Span;
} | {
    readonly code: "UnexpectedToken";
    readonly span: Span;
    readonly kind: TokenKind;
    readonly text: string;
} | {
    readonly code: "UnrecognizedToken";
    readonly span: Span;
} | {
    readonly code: "InvalidRegex";
    readonly span: Span;
} | {
    readonly code: "UnterminatedRegex";
    readonly span: Span;
} | {
    readonly code: "InvalidRange";
    readonly span: Span;
} | {
    readonly code: "InvalidHexString";
    readonly span: Span;
} | {
    readonly code: "InvalidDateFormat";
    readonly span: Span;
} | {
    readonly code: "InvalidNumberFormat";
    readonly span: Span;
} | {
    readonly code: "InvalidUr";
    readonly span: Span;
    readonly cause: string;
} | {
    readonly code: "ExpectedOpenParen";
    readonly span: Span;
} | {
    readonly code: "ExpectedCloseParen";
    readonly span: Span;
} | {
    readonly code: "ExpectedOpenBracket";
    readonly span: Span;
} | {
    readonly code: "ExpectedCloseBracket";
    readonly span: Span;
} | {
    readonly code: "ExpectedPattern";
    readonly span: Span;
} | {
    readonly code: "UnmatchedParentheses";
    readonly span: Span;
} | {
    readonly code: "UnmatchedBraces";
    readonly span: Span;
} | {
    readonly code: "InvalidCaptureGroupName";
    readonly span: Span;
    readonly name: string;
} | {
    readonly code: "InvalidPattern";
    readonly span: Span;
} | {
    readonly code: "NestingTooDeep";
    readonly span: Span;
    readonly maxDepth: number;
};

// @public
export type EnvelopePatternErrorDetailsByCode = { [D in EnvelopePatternErrorDetails as D["code"]]: D; };

// @public
export type EnvelopePatternErrorTyped<C extends EnvelopePatternErrorCode = EnvelopePatternErrorCode> = C extends EnvelopePatternErrorCode ? EnvelopePatternError & {
    readonly code: C;
    readonly details: EnvelopePatternErrorDetailsByCode[C];
} : never;

// @public
export const group: (pattern: Pattern) => Pattern;

export { Interval }

// @public
export const knownValue: (value: KnownValue | number | bigint | string) => Pattern;

// @public
export const knownValueNamed: (name: string) => Pattern;

// @public
export const knownValueRegex: (regex: RegexInput) => Pattern;

// @public
export const leaf: () => Pattern;

// @public
export type LeafPattern = {
    readonly type: "Cbor";
    readonly pattern: CborPattern;
} | {
    readonly type: "Number";
    readonly pattern: NumberPattern;
} | {
    readonly type: "Text";
    readonly pattern: TextPattern;
} | {
    readonly type: "ByteString";
    readonly pattern: ByteStringPattern;
} | {
    readonly type: "Tagged";
    readonly pattern: TaggedPattern;
} | {
    readonly type: "Array";
    readonly pattern: ArrayPattern;
} | {
    readonly type: "Map";
    readonly pattern: MapPattern;
} | {
    readonly type: "Bool";
    readonly pattern: BoolPattern;
} | {
    readonly type: "Null";
    readonly pattern: NullPattern;
} | {
    readonly type: "Date";
    readonly pattern: DatePattern;
} | {
    readonly type: "KnownValue";
    readonly pattern: KnownValuePattern;
};

// @public
export const mapWithCount: (count: number) => Pattern;

// @public
export const mapWithRange: (min: number, max?: number) => Pattern;

// @public
export const matches: (pattern: Pattern, haystack: Envelope) => boolean;

// @public
export interface MatchResult {
    readonly captures: ReadonlyMap<string, readonly Path[]>;
    readonly paths: readonly Path[];
}

// @public
export type MetaPattern = {
    readonly type: "Any";
} | {
    readonly type: "And";
    readonly patterns: readonly Pattern[];
} | {
    readonly type: "Or";
    readonly patterns: readonly Pattern[];
} | {
    readonly type: "Not";
    readonly pattern: Pattern;
} | {
    readonly type: "Search";
    readonly pattern: Pattern;
} | {
    readonly type: "Traverse";
    readonly patterns: readonly Pattern[];
} | {
    readonly type: "Group";
    readonly pattern: Pattern;
    readonly quantifier: Quantifier;
} | {
    readonly type: "Capture";
    readonly name: string;
    readonly pattern: Pattern;
};

// @public
export type NodePattern = {
    readonly variant: "Any";
} | {
    readonly variant: "AssertionsInterval";
    readonly interval: Interval;
};

// @public
export const nodeWithAssertionsCount: (count: number) => Pattern;

// @public
export const nodeWithAssertionsRange: (min: number, max?: number) => Pattern;

// @public
export const not: (pattern: Pattern) => Pattern;

// @public
export const nullValue: () => Pattern;

// @public
export const number: (value: number) => Pattern;

// @public
export const numberGreaterThan: (value: number) => Pattern;

// @public
export const numberGreaterThanOrEqual: (value: number) => Pattern;

// @public
export const numberLessThan: (value: number) => Pattern;

// @public
export const numberLessThanOrEqual: (value: number) => Pattern;

// @public
export const numberNaN: () => Pattern;

// @public
export const numberRange: (min: number, max: number) => Pattern;

// @public
export const object: (pattern: Pattern) => Pattern;

// @public
export type ObjectPattern = {
    readonly variant: "Any";
} | {
    readonly variant: "Pattern";
    readonly pattern: Pattern;
};

// @public
export const obscured: () => Pattern;

// @public
export interface ObscuredPattern {
    readonly variant: "Any" | "Elided" | "Encrypted" | "Compressed";
}

// @public
export const or: (...patterns: Pattern[]) => Pattern;

// @public
export function parseEnvelopePattern(input: string, options?: ParseOptions): Pattern;

// @public
export interface ParseOptions {
    readonly maxDepth?: number | undefined;
}

// @public
export type Path = readonly Envelope[];

// @public
export const paths: (pattern: Pattern, haystack: Envelope) => Path[];

// @public
export const pathsWithCaptures: (pattern: Pattern, haystack: Envelope) => MatchResult;

// @public
export type Pattern = {
    readonly kind: "Leaf";
    readonly pattern: LeafPattern;
} | {
    readonly kind: "Structure";
    readonly pattern: StructurePattern;
} | {
    readonly kind: "Meta";
    readonly pattern: MetaPattern;
};

// @public
export const patternEquals: (a: Pattern, b: Pattern) => boolean;

export { PatternRegex }

// @public
export const predicate: (pattern: Pattern) => Pattern;

// @public
export type PredicatePattern = {
    readonly variant: "Any";
} | {
    readonly variant: "Pattern";
    readonly pattern: Pattern;
};

export { Quantifier }

export { RegexInput }

export { Reluctance }

// @public
export const repeat: (pattern: Pattern, quantifier: Quantifier) => Pattern;

// @public
export const search: (pattern: Pattern) => Pattern;

// @public
export interface Span {
    readonly end: number;
    readonly start: number;
}

// @public
export function span(start: number, end: number): Span;

// @public
export type StructurePattern = {
    readonly type: "Leaf";
} | {
    readonly type: "Assertions";
    readonly pattern: AssertionsPattern;
} | {
    readonly type: "Digest";
    readonly pattern: DigestPattern;
} | {
    readonly type: "Node";
    readonly pattern: NodePattern;
} | {
    readonly type: "Object";
    readonly pattern: ObjectPattern;
} | {
    readonly type: "Obscured";
    readonly pattern: ObscuredPattern;
} | {
    readonly type: "Predicate";
    readonly pattern: PredicatePattern;
} | {
    readonly type: "Subject";
    readonly pattern: SubjectPattern;
} | {
    readonly type: "Wrapped";
    readonly pattern: WrappedPattern;
};

// @public
export const subject: (pattern: Pattern) => Pattern;

// @public
export type SubjectPattern = {
    readonly variant: "Any";
} | {
    readonly variant: "Pattern";
    readonly pattern: Pattern;
};

// @public
export const tagged: (tag: Tag | number | bigint | string, pattern: Pattern_2) => Pattern;

// @public
export const taggedName: (name: string, pattern: Pattern_2) => Pattern;

// @public
export const taggedRegex: (regex: RegexInput, pattern: Pattern_2) => Pattern;

// @public
export const text: (value: string) => Pattern;

// @public
export const textRegex: (regex: RegexInput) => Pattern;

// @public
export type TokenKind = "And" | "Or" | "Not" | "Traverse" | "RepeatZeroOrMore" | "RepeatZeroOrMoreLazy" | "RepeatZeroOrMorePossessive" | "RepeatOneOrMore" | "RepeatOneOrMoreLazy" | "RepeatOneOrMorePossessive" | "RepeatZeroOrOne" | "RepeatZeroOrOneLazy" | "RepeatZeroOrOnePossessive" | "Assertion" | "AssertionPred" | "AssertionObj" | "Digest" | "Node" | "Obj" | "Obscured" | "Elided" | "Encrypted" | "Compressed" | "Pred" | "Subject" | "Wrapped" | "Unwrap" | "Search" | "ByteString" | "Leaf" | "Cbor" | "DateKeyword" | "Known" | "Null" | "NumberKeyword" | "Tagged" | "BoolKeyword" | "BoolTrue" | "BoolFalse" | "TextKeyword" | "NaN" | "StringLiteral" | "ParenOpen" | "ParenClose" | "BracketOpen" | "BracketClose" | "Comma" | "Ellipsis" | "GreaterThanOrEqual" | "LessThanOrEqual" | "GreaterThan" | "LessThan" | "Integer" | "Float" | "Infinity" | "NegativeInfinity" | "GroupName" | "Regex" | "HexPattern" | "HexBinaryRegex" | "DatePattern" | "Range" | "SingleQuotedPattern" | "SingleQuotedRegex";

// @public
export const traverse: (...patterns: Pattern[]) => Pattern;

// @public
export function tryParseEnvelopePattern(input: string, options?: ParseOptions): DcborResult<Pattern, EnvelopePatternError>;

// @public
export const unit: () => Pattern;

// @public
export const unwrap: (pattern?: Pattern) => Pattern;

// @public
export const wrapped: () => Pattern;

// @public
export type WrappedPattern = {
    readonly variant: "Any";
} | {
    readonly variant: "Unwrap";
    readonly pattern: Pattern;
};

// (No @packageDocumentation comment for this package)

```
